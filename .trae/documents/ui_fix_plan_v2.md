# UI 修复计划

## 任务概述

修复 FatePilot 应用的 UI 细节问题：

1. 删除侧边栏中的"菜单"标题文字
2. 在阳历按钮后添加"（推荐）"标注
3. 修复侧边栏中"模型延迟测试"功能入口不显示的问题

## 修改文件清单

### 1. Sidebar/index.tsx

**修改内容：**

* 删除第 39 行附近的"菜单"标题 `<h2>菜单</h2>`

* 检查并修复"模型延迟测试"按钮的显示问题

**当前问题：**

* "菜单"文字显示在侧边栏顶部

* "模型延迟测试"按钮可能在隐藏条件判断中被排除

### 2. BirthForm.tsx

**修改内容：**

* 找到阳历/农历选择按钮区域

* 在"阳历"文字后添加"（推荐）"

* 修改前：`阳历`

* 修改后：`阳历（推荐）`

## 实施步骤

1. 读取 Sidebar/index.tsx，定位"菜单"标题和模型测试按钮
2. 读取 BirthForm.tsx，定位阳历选择按钮
3. 执行修改
4. 验证修改结果

## 预期结果

* 侧边栏顶部不再显示"菜单"二字

* 阳历按钮显示为"阳历（推荐）"

* 侧边栏中显示"⚡ 模型延迟

